function price(item1, item2, item3) {
  return item1 + item2 + item3;
}
// This function calculates the total price of three items

var total = price(10.99, 5.50, 3.25);

// Output to console
console.log("Total price is: R" + total);

// Output to webpage
document.write(total)= "Total price is: R" + total;
