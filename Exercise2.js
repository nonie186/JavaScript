// Step 1: Create a variable and assign it the value 2
var chapterNumber = 2;

// Step 2: Create a second variable with the string
var message = "This is chapter number: ";

// Step 3: Concatenate the string and number
var fullMessage = message + chapterNumber;

// Step 4: Output it to the browser window
document.write(fullMessage);
